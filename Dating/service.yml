apiVersion: v1
kind: Service
metadata:
  name: dating-app-service
  labels:
    app: dating-app-service
spec:
  selector:
    app: dating-app
  ports:
    - protocol: TCP
      port: 80          # Service port
      targetPort: 8080  # forwards to containerPort inside pod
  type: NodePort
